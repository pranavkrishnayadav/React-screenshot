let url = 'http://localost:8080'

let flag  = true;
let score = 88;

let db_config = {

    host:"local host",
    user:"root",
    password:"root",
    database:"modedb",
    table:"products"
};

export default url;

export{flag,score,db_config};